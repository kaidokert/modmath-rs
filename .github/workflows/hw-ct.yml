name: hw-ct

on:
  push:
    branches:
      - main
      - 'feature/**'
  pull_request:
  workflow_dispatch:

jobs:
  hardware:
    uses: ./.github/workflows/reusable-hw-ct.yml
    with:
      campaigns-json: '["modmath-jtrace-f407"]'
      working-directory: ct-verify/cyccnt-hardware
      prepared: true
